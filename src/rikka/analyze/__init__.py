"""解析機能をまとめるサブパッケージ。

役割:
    通常 PDR、particle filter、センサー可視化を ``rikka.analyze`` 名前空間の下に
    配置するためのパッケージ境界を定義する。
依存元:
    このファイル自体は他モジュールを import せず、副作用を持たない。
利用先:
    CLI や外部コードが ``rikka.analyze.pdr``、``particle_filter``、
    ``sensor_plot`` を読み込む際に Python のパッケージ初期化として使用される。
処理フロー:
    個別の解析処理は各子モジュールが担当し、このファイルでは処理を実行しない。
"""
