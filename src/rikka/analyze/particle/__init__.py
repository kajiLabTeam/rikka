"""パーティクルフィルタ内部実装の責務別パッケージ。

役割:
    粒子診断、再標本化、運動モデル、地図制約、軌跡復元、復旧、描画、実行統括を
    責務ごとに提供する。
依存元:
    各サブモジュールが ``config`` と ``pdr.particle_api`` から必要な設定値と共有
    APIを取得する。
利用先:
    ``rikka.analyze.particle_filter`` の互換 facade と粒子フィルタ実行処理から使う。
処理フロー:
    実行処理が各サブモジュールを直接 import し、粒子の伝播から地図判定、軌跡
    復元までを順に組み立てる。本モジュール自体は公開名を再輸出しない。
"""
